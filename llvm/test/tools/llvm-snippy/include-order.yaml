# RUN: llvm-snippy %S/Inputs/include-order-1.yaml %S/Inputs/include-order-3.yaml -march=riscv64-linux-gnu -num-instrs=10 -model-plugin=None
# RUN: llvm-snippy %S/Inputs/include-order-3.yaml %S/Inputs/include-order-1.yaml -march=riscv64-linux-gnu -num-instrs=10 -model-plugin=None

# Check that include preprocessing does not depend on the order
# of layout files passed on the command line.
